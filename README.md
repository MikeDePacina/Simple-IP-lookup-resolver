Portable(Winsocket and Berkeley socket) IP lookup command line app written in C


To use:

run executable file and add hostname to lookup IP addresses associated to it


Example:
./ip_lookup github.com
